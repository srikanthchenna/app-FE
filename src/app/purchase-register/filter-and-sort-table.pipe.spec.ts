import { FilterAndSortTablePipe } from './filter-and-sort-table.pipe';

describe('FilterAndSortTablePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterAndSortTablePipe();
    expect(pipe).toBeTruthy();
  });
});
